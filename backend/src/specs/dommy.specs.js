import {
  describe,
  before,
  after,
  it,
} from 'mocha';
import { expect } from 'chai';

describe('helloeworld Server', () => {
  before(async () => { });
  after(async () => {
  });
  it('Should create a account', (done) => {
    expect(true).to.eq(true);
    done();
  });
});
